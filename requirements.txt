# Core (required to run the mock pipeline and tests)
pydantic>=2.0
pytest>=7.0

# Optional - only needed for real LLM calls / rendering. The codebase imports
# and runs in mock mode without these installed.
# anthropic>=0.40
# openai>=1.0
# manim>=0.18
